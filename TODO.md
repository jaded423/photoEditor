# photoEditor TODO

- [x] **Rebuild + redistribute PhotoEditor.app with the focal cut** (added 2026-09-04) — DONE 2026-09-04: rebuilt from `385d894`+tuning commit, `dist/PhotoEditor.zip` (133MB, sha256 e42b0ced…) written over Drive `Product Pics and Vids/PhotoEditor/PhotoEditor.zip` (same file ID `1lYKUEKkYuxqeKA9tPAtJjoo1VXgiX-K_`, Drive keeps it as a new version). Joshua has Cynthia download it next week; recipient runs `xattr -cr PhotoEditor.app`; first run downloads ~375MB of SAM weights next to BiRefNet.
- [ ] **Fresh `Elevated-Trading-LLC/photoEditor` — clean PUBLIC org repo, code + docs only, v1.0.0 re-cut (added 2026-09-24, Joshua: "add photoEditor fresh, leaving 2 clean repos").** Steps (1)–(5) DONE 2026-09-24: root commit `dd21b6b` (author `Joshua Brown <j@jadedviber.com>`, no trailer), 15 files, README rewritten in the elevatedCore shape, INSTALL.md minus the Drive-era "Google can't scan" screenshot, LICENSE = MIT `Copyright (c) 2026 Joshua Brown <j@jadedviber.com>` (Joshua's call: his IP, his name ONCE per repo, nowhere else); public, not a fork, branch `master`; release `v1.0.0` = `release/PhotoEditor.zip`, served sha256 e42b0ced… Stale local remote `elevated` removed.
      resume: only (6) left — collaborators Cesar (`cesgarma`) admin, Cody (`CSandone`) + Cynthia (`cyncast86`) read. The auto-mode classifier blocks Claude granting repo access → Joshua runs it: `for u in CSandone cyncast86; do gh api -X PUT repos/Elevated-Trading-LLC/photoEditor/collaborators/$u -f permission=pull; done; gh api -X PUT repos/Elevated-Trading-LLC/photoEditor/collaborators/cesgarma -f permission=admin`
      verify: `gh repo list Elevated-Trading-LLC --json name -q '.[].name'` → exactly elevatedCore + photoEditor · `curl -sIL https://github.com/Elevated-Trading-LLC/photoEditor/releases/latest/download/PhotoEditor.zip | grep -ciE '^HTTP/[0-9.]+ 200'` ≥ 1 (HTTP/2 prints no "OK") · `git -C <clone> log --oneline | wc -l` → 1 · `grep -rli "joshua\|jadedviber\|claude" <clone> --exclude-dir=.git | grep -v /LICENSE$` → nothing · `gh api repos/Elevated-Trading-LLC/photoEditor/collaborators -q '.[].login'` → cesgarma CSandone cyncast86 jaded423
- [ ] **Optional: red colour cast from the bucket** (added 2026-09-04). The red tray reflects onto the bud; a gentle bud-only white balance (gray-world on the matte, capped) would neutralise it. Cosmetic; decide after seeing the new cutouts on the site.
- [x] **Ship PhotoEditor via GitHub Releases on the Elevated org** (added 2026-09-15) — DONE 2026-09-15: `v1.0.0` on `Elevated-Trading-LLC/photoEditor`, same zip as Drive (sha256 e42b0ced…); INSTALL.md/html/pdf point at `…/releases/latest/download/PhotoEditor.zip`. Cynthia to install from that link.

- [x] **Gitignore `.claude/`** — `.claude/settings.local.json` (local machine settings) shows as untracked on every push-all sweep. Add `.claude/` to `.gitignore`. Elevated-Trading-LLC repo (branch `main`) → PR workflow, not direct push. (surfaced by push-all 2026-06-20) — DONE (`.claude/` present in .gitignore; no longer shows as untracked)
